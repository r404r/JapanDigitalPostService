# Implementation Plans

Generated by the `improve` skill on 2026-06-12 (commit `ad2cad8`).
Execute in the order shown below unless dependencies say otherwise.
Each executor: read the plan fully before starting, honour its STOP conditions,
and update the Status column when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| [001](001-lock-release-context.md) | Fix lock `release` closure missing bounded context | P1 | S | — | DONE (已由当前 main 上游实现) |
| [002](002-ci-web-job.md) | Wire CI web job (frontend build + vitest) | P1 | S | — | DONE |
| [003](003-stale-lock-ttl-test.md) | Test stale-lock TTL reclamation | P1 | S | 001 | DONE |
| [004](004-like-escape-test.md) | Test LIKE `_` and `\` literal escape | P2 | S | — | DONE |
| [005](005-ci-caching.md) | Add Go module & npm caching to CI | P2 | M | 002 | DONE |
| [006](006-dx-polish.md) | DX polish: STATIC_DIR env doc + make help + writeJSON logging | P2 | S | — | DONE |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED (<reason>)` | `REJECTED (<rationale>)`

## Dependency notes

- **003 after 001**: the TTL test deliberately leaves a stale lock; if 001 is not yet merged, the release in the test will use the un-fixed closure (no context), which may cause the test to hang in a slow-DB scenario. Merge 001 first, or run both on a fresh in-memory SQLite where hangs are impossible in practice.
- **005 after 002**: the npm cache step added in 005 targets the same `openapi` and new `web` jobs. Wire up the web job first so the cache key path (`web/package-lock.json`) is correct.
- Plans 001, 002, 004, 006 have no dependencies and can be executed in any order or in parallel.

## Findings considered and rejected

- **All security audit items** (token comparison, nonce generation, scope model, error response safety, key handling): all verified as correctly implemented — no findings.
- **Correctness — LastUsedAt updated after DB failure** (`auth/service.go:113`): intentional "best-effort" audit field per spec §5; not a bug.
- **Offset silently applied when `total > MAX_TOTAL`**: spec language is ambiguous ("only valid when"); current behaviour is well-defined. Not a correctness bug; no plan needed.
- **Dual COUNT+SELECT query per search**: by-design architecture (count validates threshold before materialising result set). Address only if query latency becomes measurable.
- **Rate limiting missing**: deferred to a deployment-layer concern (reverse proxy / load balancer). `rate_limited` status code is spec-reserved for future use.
- **FTS upgrade path** (pg_trgm / MySQL ngram / FTS5): acknowledged in architecture §6; 124 k rows is fast enough with LIKE + indexes for current load. Plan a spike only when query latency is actually observed.
- **Token audit endpoint**: additive feature; no urgency until token-usage observability is needed.
